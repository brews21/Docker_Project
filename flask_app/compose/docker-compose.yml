services:
  flask-app:
    build:
      context: ..
      dockerfile: dockerfiles/flask-app.Dockerfile
    image: flask-docker-example:local
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: production
